public class Position {
    int positionID;
    public enum positionName{
        Dev, Test, Scrum_Master, PM;
    }
    positionName positionName;
//  kieu du lieu enum(positionName) bien du lieu(positionName)
}
